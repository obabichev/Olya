'use strict';

const initState = {
    refreshToken: null,
    accessToken: null,
};

const auth = (state = initState, action) => {
    switch (action.type){
        default:
            return state;
    }
};

export default auth;